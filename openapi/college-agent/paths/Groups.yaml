get:
  summary: List Group by ID
  operationId: list-Group-id
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/Group.yaml
